import 'package:flutter/material.dart';

class VSizeBox extends StatelessWidget {
  const VSizeBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 16.0,
    );
  }
}
